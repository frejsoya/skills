id: improve-codebase-architecture-negative-001
name: improve-codebase-architecture negative trigger (no skill)
description: An unrelated prompt where no skill (and definitely not improve-codebase-architecture) should fire.
tags: [negative-trigger, routing]
inputs:
  prompt: "What's the weather tomorrow?"
graders:
  - type: skill_invocation
    name: no_skill_fires
    config:
      mode: exact_match
      required_skills: []
      allow_extra: false
