# Routing eval for the 'to-prd' skill — does the right skill fire for a prompt?
name: to-prd-routing-eval
description: |
  Trigger/routing accuracy for the to-prd skill: positive prompts must invoke it,
  confusable prompts must not. Generated from evals/trigger-cases.md.
skill: to-prd
version: "1.0"

config:
  trials_per_task: 1
  timeout_seconds: 300
  parallel: false
  skill_directories: [skills/engineering]   # where waza finds the nested skill (run from repo root)
  executor: mock          # switch to copilot-sdk for real model-graded routing
  model: claude-haiku-4-5  # cheap default; override per-run: make waza MODEL=claude-sonnet-4-6

metrics:
  - name: trigger_accuracy
    weight: 1.0
    threshold: 0.9
    description: Fires on positive prompts, stays silent on negative ones.

tasks:
  - "tasks/*.yaml"
