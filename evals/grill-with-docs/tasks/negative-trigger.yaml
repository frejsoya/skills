id: grill-with-docs-negative-001
name: grill-with-docs negative trigger (no skill)
description: An unrelated prompt where no skill (and definitely not grill-with-docs) should fire.
tags: [negative-trigger, routing]
inputs:
  prompt: "What's the weather tomorrow?"
graders:
  - type: skill_invocation
    name: no_skill_fires
    config:
      mode: exact_match
      required_skills: []
      allow_extra: false
